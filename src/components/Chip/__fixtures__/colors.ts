// import { IChipProps } from '../Chip.props'

import { ChipsColor, IChipProps } from "../Chip.props"

export const colors : Record<ChipsColor , IChipProps['color']> = {
  primary: 'primary',
  secondary: 'secondary',
  default: 'default'
}

export default colors
